import { TextClassNames } from "../styles/CoreClassNames";
import { getSpacingContainerClassName } from "../styles/Spacing";

export const formStyles = {
    actionsContainer: TextClassNames.alignRight,
    standardSpacingContainer: getSpacingContainerClassName("y", 20),
};
